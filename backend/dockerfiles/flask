FROM radiobretzel/backend:base

RUN addgroup -S rb-backend && adduser -S -g rb-backend rb-backend

RUN pip install --upgrade pip

USER rb-backend
ENV PATH="/home/rb-backend/.local/bin:${PATH}"

RUN pip install --user Flask

COPY hello.py /hello.py

CMD ["/hello.py"]
