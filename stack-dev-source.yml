version: "3.3"

services:
  backend:
    command: ./test.liq
    image: radiobretzel/source:dev
    build:
      context: ./source/
      dockerfile: dockerfiles/dev
    ports:
      - 8080:8080
    volumes:
      - ./source/scripts:/scripts
