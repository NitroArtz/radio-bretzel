FROM radiobretzel/stream:icecast

LABEL project.component="Radio Bretzel Stream server"
LABEL project.version="0.0.2"
LABEL project.env="dev"
LABEL docker.image.name="radiobretzel/stream:dev-0.0.2"
LABEL docker.image.version="0.1"
LABEL docker.image.maintainer="Papy Bretzel <papybretzel@ntymail.com>"

VOLUME /etc/icecast2/

EXPOSE 8000

USER icecast2
WORKDIR /etc/icecast2/

ENTRYPOINT ["icecast2", "-c"]
CMD ["default.xml"]
